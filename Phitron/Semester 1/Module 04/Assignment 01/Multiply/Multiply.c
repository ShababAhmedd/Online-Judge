#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    //int A, B;
    long long int mul, A, B;
    scanf("%lld %lld", &A, &B);
    mul = A * B;
    printf("%lld", mul);
    return 0;
}
